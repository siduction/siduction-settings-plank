#!/bin/sh
# fluxbox plank starter

if [ ! -d $HOME/.config/plank/fluxbox ]; then
    mkdir -p $HOME/.config/plank/fluxbox
    cp -rf /usr/share/siduction-settings-plank/profiles/fluxbox $HOME/.config/plank/
fi

sleep 3
pkill -9 -x plank
plank --name fluxbox
